import 'package:flutter/material.dart';

class AppColors {
  // Brand
  static const Color primary = Color(0xFF3F51B5); // indigo-ish
  static const Color accent = Color(0xFFFF6F62); // brand orange used for buttons etc.

  // Text
  static const Color textPrimary = Color(0xFF212121); // near-black for primary text
  static const Color textSecondary = Color(0xFF6B6B6B); // secondary text
  static const Color textMuted = Color(0xFF9E9E9E);

  // Backgrounds
  static const Color background = Color(0xFFFFFFFF);
  static const Color scaffoldOverlay = Color(0xfff8f9fa);

  // Surface / cards
  static const Color surface = Color(0xFFFFFFFF);

  // Danger / negative
  static const Color danger = Color(0xFFB00020);
}

